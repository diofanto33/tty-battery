#include <stdio.h>
#include <stdlib.h>

#include "tty_battery.h"

const char * BATTERY_STATUS_S[6] = {"FULL", "HIGH", "NORMAL", "LOW", "CRITICAL", "UNKNOWN"};


void draw_number(int n, int x, int y) {
    n = n;
    x = x;
    y = y;
     return;
}
